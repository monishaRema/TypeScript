let theme = undefined;

theme = 'dark them'

const selectedTheme = theme ?? 'Light Theme';
const sTheme = theme ? theme : 'light Theme'
const aTheme = theme || 'light theme'

console.log(selectedTheme)
console.log(sTheme)
console.log(aTheme)